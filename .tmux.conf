# Forward bell to the local terminal so Claude Code Stop-hook notifications work.
# herd SSHes into Ona environments; the Stop hook writes \a to /dev/tty,
# tmux propagates it through the connection, and the local terminal handles it.
set -g monitor-bell on
set -g bell-action any
set -g visual-bell off
